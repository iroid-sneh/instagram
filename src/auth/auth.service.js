class authService {
    /**
     * @description: User Signup
     * @param {*} req
     * @param {*} res
     */
    static async signup(req, res) {
        try {
        } catch (error) {}
    }
}

export default authService;
